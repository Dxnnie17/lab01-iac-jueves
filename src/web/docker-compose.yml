dockerversion: '3.8'

services:
  web01:
    build:
      context: ./web01
      dockerfile: Dockerfile
    container_name: web01-container
    ports:
      - "4000:80"
    restart: always

  web02:
    build:
      context: ./web02
      dockerfile: Dockerfile
    container_name: web02-container
    ports:
      - "4001:80"
    restart: always
